# TP-SL Planner (Open Core)

This package is a reorganized version of your TP-SL Planner with a modular,
plugin-friendly structure. Run with:

```bash
python -m tpsl_planner
```

or install in editable mode and run:

```bash
pip install -e .
tpsl-planner
```